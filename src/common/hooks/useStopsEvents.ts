const stops = [
  {
    resourceId: 'c',
    title: 'event 33',
    start: Date.now(),
    end: Date.now(),
    type: 'stop',
    color: 'transparent',
  },
];

const useStopsEvents = () => {
  return stops;
};

export default useStopsEvents;
